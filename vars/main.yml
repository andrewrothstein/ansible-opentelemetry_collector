---
# vars file for opentelemetry_collector
otelcol_os: '{{ ansible_system | lower }}'
otelcol_arch: '{{ otelcol_arch_map[ansible_architecture] }}'
otelcol_platform: '{{ otelcol_os }}_{{ otelcol_arch }}'
